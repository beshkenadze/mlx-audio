"""ZONOS2 — sparse-MoE multi-codebook autoregressive TTS (MLX port).

The top-level ``Model`` class is added during integration (Phase D). Sub-modules
(``config``, ``layers``, ``moe``, ``tokenizer``, ``speaker``, ``speaker_encoder``,
``sampling``, ``convert``) are implemented independently per ``CONTRACT.md``.
"""
